{
  "uitleg": "Uitslag van scripts/sabotage.js: per wet of het uitzetten van zijn handhaver een genoemde toets rood maakt. Gegenereerd, niet met de hand bij te werken.",
  "wetten": {
    "RTG-003": {
      "stand": "BEWEZEN",
      "uitleg": "server/accounts/tokens.js: const TOKENVORM = /^[A-Za-z0-9_-]+\\.[a-f0-9]{32}$/; -> const TOKENVORM = /^[\\s\\S]+$/; maakt tokenvorm.test.js rood"
    },
    "RTG-004": {
      "stand": "BEWEZEN",
      "uitleg": "server/accounts/tokens.js: if (u && Number(u.sessies_vanaf || 0) > Number(uitgegeven || 0)) return null; -> if (false) return null; maakt sessiegrens.test.js rood"
    },
    "RTG-009": {
      "stand": "BEWEZEN",
      "uitleg": "server/middleware/remmen.js:     if (klaar) return next(); ->     return next(); maakt middleware.test.js rood"
    },
    "RTG-010": {
      "stand": "BEWEZEN",
      "uitleg": "server/accounts/gebonden.js: return Buffer.from('rtg-kluis-v2|' + TABEL + '|' + kolom + '|' + String(id), 'utf8'); -> return Buffer.from('rtg-kluis-v2|' + TABEL, 'utf8'); maakt kluis-binding.test.js rood"
    },
    "RTG-013": {
      "stand": "BEWEZEN",
      "uitleg": "server/inzagelog.js: function noteer({ door, over, waarom, bron } = {}) { -> function noteer({ door, over, waarom, bron } = {}) {\n  if (1) return null; maakt inzagelog.test.js rood"
    },
    "RTG-014": {
      "stand": "BEWEZEN",
      "uitleg": "server/log.js: p: req.path, s: res.statusCode -> p: req.originalUrl, s: res.statusCode maakt loghygiene.test.js rood"
    },
    "RTG-015": {
      "stand": "BEWEZEN",
      "uitleg": "server/kern/ssrf.js: function veiligeExternalUrl(url) { -> function veiligeExternalUrl(url) {\n  return { ok: true }; maakt ssrf.test.js rood"
    },
    "RTG-023": {
      "stand": "BEWEZEN",
      "uitleg": "server/kern/spellen/grens.js: progressieMag: (handle) => volwassen(handle), -> progressieMag: (handle) => true, maakt speldag.test.js rood"
    },
    "RTG-036": {
      "stand": "BEWEZEN",
      "uitleg": "scripts/zekerheid.js: zin: niveau + ' -- NIET ABSOLUUT (' + onbekend + ' onbekend)' -> zin: niveau maakt zekerheid.test.js rood"
    },
    "RTG-037": {
      "stand": "BEWEZEN",
      "uitleg": "scripts/uitzonderingen.js:   if (dagen < 0) return { stand: 'VERLOPEN', mist: [], dagen }; ->   if (false) return { stand: 'VERLOPEN', mist: [], dagen }; maakt uitzonderingen.test.js rood"
    }
  }
}
