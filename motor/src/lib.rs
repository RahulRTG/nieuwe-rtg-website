/* RTG Motor — de hete, security-kritische kern in Rust. Zero-dependency
   (alleen std). De bibliotheek stelt de bouwstenen beschikbaar; de binaire
   `main` zet er de HTTP-motor omheen. */
pub mod grootboek;
pub mod json;
pub mod pay;
pub mod rng;
pub mod http;
