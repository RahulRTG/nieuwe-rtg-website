# Het gezaaide canvas is een bouwsel van ~2,5 MB (de editor zit erin). Bron is
# delen/*.part.html + _stijl.css + canvas.json; `node _bouw.mjs` maakt de
# .dc.html-bestanden.
rtg-muziek-sociaal-zorg.html
